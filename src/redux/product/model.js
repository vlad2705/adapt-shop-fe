// @flow
import Immutable from 'seamless-immutable'

import type { ProductDetailState, ProductResponseState } from '../../types'

export const initialProductsState: Immutable<ProductResponseState> = Immutable({})

export const initialProductState: Immutable<ProductDetailState> = Immutable({})