// @flow
import Immutable from 'seamless-immutable'

import type { RegistrationState } from '../../types'

const initialState: Immutable<RegistrationState> = Immutable({})

export default initialState